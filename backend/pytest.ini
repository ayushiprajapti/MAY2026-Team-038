[pytest]
pythonpath = .
markers =
    live: hits the real database and real external APIs (costs money, non-deterministic) - excluded by default, run explicitly with `pytest -m live`
addopts = -m "not live"
